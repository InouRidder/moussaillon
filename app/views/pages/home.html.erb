<!-- Content -->

<style>
.videoWrapper {
  position: relative;
  padding-bottom: 56.25%; /* 16:9 */
  padding-top: 25px;
  height: 100%;
  z-index: 1;
}

.videoWrapper iframe {
  position: absolute;
  top: 0;
  left: 0;
  min-width: 100%;
  min-height: 100%;
  width: 100vw;
  height: auto;
}
</style>


<section class="video-wrapper">
  <!-- Copy & Pasted from YouTube -->
  <iframe style="pointer-events: none;" width="100%" height="600px" src="https://www.youtube.com/embed/9PKjNUr_LYo?autoplay=1&modestbranding=1&autohide=1&showinfo=0&controls=0" frameborder="0"></iframe>
</section>

<!-- Knowledge Share -->
<section class="light-gray-bg padding-top-150 padding-bottom-150">
  <div class="container">

    <!-- Main Heading -->
    <div class="heading text-center">
      <h4>
        your place
        <br>
        YOUR DIMENSIONS
        <br>
        OUR DESIGN
        <br>
      </h4>
      <p>
        MOUSSAILLON - FURNITURE MAKERS
      </p>
    </div>
        <div class="heading text-center padding-top-150 padding-bottom-50">
        <h5>
          Our latest work
        </h5>
        </div>
    <div class="knowledge-share">
      <ul class="row">
        <div class="shop-items row">

          <% @latest_products.each do |product| %>

          <!-- Item -->
          <div class="col-xs-12 col-sm-6">
            <div class="item">
              <!-- Item Name -->
              <div class="item-name"> <a href="#."><%= product.name %></a>
                <p><%= product.description %></p>
                <p> Added on <%= product.created_at.strftime("%A, %b %d") %></p>
              </div>
              <!-- Item img -->
              <%= link_to product_path(product) do %>
              <div class="item-img">
                <img class="img-1" src=<%= cl_image_path product.photos.first.path unless product.photos.blank? %> alt="" style="width: 100%; height: 400px;">
                <div class="inn">
                  <a href="<%= product_path(product) %>"> Show <a href="#.">
                  </a>
                </div>
              </div>
              <% end %>
              <!-- Price -->
            </div>
          </div>
          <% end %>


        </ul>
      </div>
    </div>
  </section>



  <script>
  // Find all YouTube videos
  var $allVideos = $("iframe[src^='//www.youtube.com']"),

    // The element that is fluid width
    $fluidEl = $("body");

// Figure out and save aspect ratio for each video
$allVideos.each(function() {

  $(this)
  .data('aspectRatio', this.height / this.width)

    // and remove the hard coded width/height
    .removeAttr('height')
    .removeAttr('width');

  });

// When the window is resized
$(window).resize(function() {

  var newWidth = $fluidEl.width();

  // Resize all videos according to their own aspect ratio
  $allVideos.each(function() {

    var $el = $(this);
    $el
    .width(newWidth)
    .height(newWidth * $el.data('aspectRatio'));

  });

// Kick off one resize to fix all videos on page load
}).resize();

var player =  iframe.getElementById('player');
player.mute();

</script>
