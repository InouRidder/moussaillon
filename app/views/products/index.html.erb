        <style>
        @media (min-width: 850) {
            .side-bar-nav {
              display: flex;
              flex-direction: column;
              position: fixed;
              z-index: 99;
              top: 40%;
              right: 0px;
            }
            .side-bar-nav a {
              padding: 5px 10px;
              min-width: 80px;
              background-color: white;
            }
          }
          @media (max-width: 850) {
            .side-bar-nav {
              display: none;
            }
          }
        </style>

        <div class="side-bar-nav">
          <%= link_to 'All', collection_path %>
          <%= link_to 'Sleep', collection_path(type: "sleep") %>
          <%= link_to 'Eat', collection_path(type: "eat") %>
          <%= link_to 'Living', collection_path(type: "living" ) %>
          <%= link_to 'Cook', collection_path(type: "cook" ) %>
          <%= link_to 'Sit', collection_path(type: "sit" ) %>
        </div>
 <!-- Popular Products -->
 <section class="shop-page padding-top-50 padding-bottom-200" style="padding-right: 5%">
  <div class="container">
    <div class="item-display">

      <div class="row">
        <div class="col-xs-12">
          <div class="text-center">
            <h2>
            <%= @info_title %>
            </h2>
          </div>
        </div>
        <div class="col-xs-6">
          <span class="product-num">
            Showing 1 - of <%= @products.count %> products
          </span>
        </div>

        <!-- Products Select -->



      </div>

      <!-- Popular Item Slide -->

      <div class="shop-items row">

        <% @products.each do |product| %>

        <!-- Item -->
        <div class="col-md-6">
          <div class="item">
            <!-- Item Name -->
            <div class="item-name"> <a href="#."><%= product.name %></a>
              <p><%= product.description %></p>
            </div>
            <!-- Item img -->
            <%= link_to product_path(product) do %>
            <div class="item-img">
              <img class="img-1" src=<%= cl_image_path product.photos.first.path unless product.photos.blank? %> alt="" style="width: 100%; height: 400px;">
              <div class="inn">
                <a href="<%= product_path(product) %>"> Show <a href="#.">
              </a>
            </div>
          </div>
          <% end %>
          <!-- Price -->
          <span class="price"><small>€</small><%= product.price %></span>
        </div>
      </div>
      <% end %>

      <!-- Pagination -->
<!--         <ul class="pagination in-center">
          <li><a href="#"><i class="fa fa-angle-left"></i></a></li>
          <li class="active"><a href="#">1</a></li>
          <li><a href="#">2</a></li>
          <li><a href="#">3</a></li>
          <li><a href="#"><i class="fa fa-angle-right"></i></a></li>
        </ul> -->

      </div>
    </div>
  </div>
</section>

